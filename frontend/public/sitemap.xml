<?xml version="1.0" encoding="UTF-8"?>
<!--
  Only the homepage is listed: team/match/player pages use IDs from
  football-data.org/API-Football that aren't known ahead of time, so they
  can't be enumerated in a static sitemap. A dynamic sitemap (generated by
  the backend from the competitions/teams it already fetches) would be the
  next step if deeper indexing is needed.
-->
<urlset xmlns="http://www.sitemap.org/schemas/sitemap/0.9">
  <url>
    <loc>https://football-info.example.com/</loc>
    <changefreq>hourly</changefreq>
    <priority>1.0</priority>
  </url>
</urlset>
